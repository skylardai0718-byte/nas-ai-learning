{
  "name": "nas-ai-learning",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "12 周 NAS、Self-hosting 与本地 AI 学习平台",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "quiz:validate": "node scripts/validate-quiz.mjs",
    "check:leak": "node scripts/check-private-leak.mjs",
    "check": "npm run quiz:validate && npm run docs:build && npm run check:leak"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "vitepress": "^1.6.4"
  }
}
